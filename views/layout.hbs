<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{title}}</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <img src="/motley-full.png">
  {{#if message}}<p><strong style="color:red">{{{message}}}</strong></p>{{/if}}
  {{{content}}}
  <hr>
  <address>Served by worker {{workerId}}</address>
</body>
</html>